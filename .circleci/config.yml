version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.5.0_1
      - image: circleci/mysql:5
    steps:
      - checkout
      - run:
          name: Greeting
          command: echo Hello, world.
      - run: npm install -g npm
      - run: npm test


