version: 2
machine:
  post:
    - ./daemon:
          background: true

jobs:
  build:
    docker:
      - image: circleci/node:10.5
      - image: circleci/postgres:10-alpine-postgis
    steps:
      - checkout
      - run:
          name: Greeting
          command: echo Hello, world.
      - run: npm install
      - run: npm test


