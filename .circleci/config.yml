version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
      - image: circleci/node:10.5.0
      - image: circleci/mysql:5
    steps:
      - checkout
      - run:
          name: Greeting
          command: echo Hello, world.
      - run: npm install
      - run: npm test


